// Route only. The screen lives in src/screens so its test can sit beside it:
// every file under src/app is a route, test files included, and one would
// otherwise be bundled into the app as a navigable screen.

import { EnterCode } from "@/screens/visit/EnterCode";

export default EnterCode;
